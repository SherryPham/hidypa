__pycache__
evaluation_results
venv
slurm_out
#demonstration
evaluation_results_lbit
random_notes.txt

# Helper script for renaming (one-time use)
helper_scripts/rename_hierarchical_to_hidypa.py

# =============================================================================
# Environment-specific config files (contain user paths and HPC account IDs)
# Keep *.example files tracked - they are templates
# =============================================================================
config/hpc_paths.sh
config/paths.py

# =============================================================================
# Evaluation output directories (contain identifying paths in JSON files)
# These are generated outputs and should not be committed
# =============================================================================

evaluation_2_backup/
COMMANDS.md
test_evaluation/